# RetV

Reactive Toolforming Voyager.

RetV is a process model for building tool-calling agents that operate safely over messy task landscapes.

The goal is not autonomy for its own sake. The goal is bounded agency:
agents that can use tools, observe results, recover from drift, and produce auditable reports without silently mutating the world.
